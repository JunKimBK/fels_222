<% provide :title, t("name_show") %>
<div class="container">
  <div class="row text-center">
    <section class="user_info">
    <h1><%= @category.name %></h1>
    <td><%= image_tag @category.picture.url %></td>
    </section>
  </div>
</div>
