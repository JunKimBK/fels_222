<% provide :title, t("all_users") %>
<h1><%= t"all_users" %></h1>
<%= will_paginate %>
<ul class="users">
  <div class="container">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th><%= t"avatar" %></th>
          <th><%= t"username" %></th>
          <th><%= t"delete" %></th>
        </tr>
      </thead>
      <tbody>
        <%= render @users %>
      </tbody>
    </table>
  </div>
</ul>
<%= will_paginate %>
